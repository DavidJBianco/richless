[project]
name = "mdless"
version = "0.1.0"
description = "A terminal paginator for Markdown files, built on less"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=14.2.0",
]

[project.scripts]
mdless = "mdless:main"
